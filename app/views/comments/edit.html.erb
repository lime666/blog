<p id="notice"><%= notice %></p>
  <h1>Edit Comment</h1>
    <%= render 'form', patch: @comment %>
      <div class="pt-2">
        <%= link_to 'Back', post_path(@post), class: 'btn btn-light ml-3' %>
      </div>
